# Restore AKS production resilience

An agent reduced the production system pool to one node. Restore an availability-zone-aware, autoscaling configuration with at least two normal nodes and capacity for four or more. Keep the modeled cost delta within $1,400/month and do not authorize `0.0.0.0/0` to the API server.
