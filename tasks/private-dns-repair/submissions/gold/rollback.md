# DNS rollback

Remove the new VNet link, restore the prior private DNS link configuration, flush application DNS caches, and verify both hostname resolution and database connectivity from the application subnet.
